<div class='header'>
  <h1>Expenses</h1>
  <%= link_to 'New expense', new_expense_path, class: 'btn btn--primary' %>
</div>

<% if @expenses.any? %>
  <%= render @expenses %>
<% else %>
  <div>No expenses available</div>
<% end %>
